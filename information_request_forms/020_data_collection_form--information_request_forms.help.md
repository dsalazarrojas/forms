<thinking>As a user-facing help guide, this form is intended to collect various types of information from users, including contact details, demographic information, and other data points. The purpose of this form is to gather relevant and accurate data that can be used for analysis, reporting, or other business purposes. The form includes a mix of required and optional fields, with some fields having specific formatting or validation rules to ensure data quality. Before completing the form, please review the instructions carefully to ensure you provide accurate and complete information.</thinking>

# data_collection_form - Help Guide

## Purpose
The data_collection_form is designed to collect various types of information from users. This form is used for data collection and analysis.

## How To Complete This Form

1.  Review the form instructions and ensure you have understood its purpose.
2.  Fill out the form fields as required or as prompted.
3.  Verify the accuracy and completeness of your input before submitting.

## Field-by-Field Explanation

* **Phone Number** (`phone_number`, `text`, **required**): Enter your phone number in the international format (e.g., +1234567890).
* **Email Address** (`email_address`, `email`, **required**): Enter a valid email address.
* **Date of Birth** (`date_of_birth`, `date`, **required**): Enter your date of birth in YYYY-MM-DD format (e.g., 1999-01-01).
* **Time of Birth** (`time_of_birth`, `time`, **required**): Enter your time of birth in 24-Hour Clock format (e.g., 12:00).
* **Notes** (`notes`, `note`, **optional**): Add any additional comments or information you'd like to include.
* **Select One Option** (`select_option`, `select_one`, **required**): Choose one option from the list.
* **Select Multiple Options** (`select_multiple`, `select_multiple`, **required**): Choose one or more options from the list.
* **Decimal Input** (`decimal_input`, `number`, **optional**): Enter a decimal value.
* **Time Input** (`time_input`, `time`, **required**): Enter a time in 24-Hour Clock format (e.g., 12:00).
* **Date Input** (`date_input`, `date`, **required**): Enter a date in YYYY-MM-DD format (e.g., 1999-01-01).
* **Text Input** (`text_input`, `text`, **required**): Enter a text value.
* **Integer Input** (`integer_input`, `number`, **required**): Enter an integer value.
* **Boolean Input** (`boolean_input`, `text`, **optional**): Choose a boolean value (e.g., true, false).
* **Select Text** (`select_text`, `select_multiple`, **optional**): Choose one or more options from the list.
* **Boolean Input 2** (`boolean_input_2`, `text`, **optional**): Choose a boolean value (e.g., true, false).
* **Decimal Input 2** (`decimal_input_2`, `number`, **optional**): Enter a decimal value.
* **Text Area Input** (`text_area_input`, `text`, **optional**): Enter a text value.
* **Integer Input 2** (`integer_input_2`, `number`, **optional**): Enter an integer value.
* **Select Multiple 2** (`select_multiple_2`, `select_multiple`, **optional**): Choose one or more options from the list.
* **Time Input 2** (`time_input_2`, `time`, **optional**): Enter a time in 24-Hour Clock format (e.g., 12:00).
* **Date Input 2** (`date_input_2`, `date`, **optional**): Enter a date in YYYY-MM-DD format (e.g., 1999-01-01).
* **Boolean Input 3** (`boolean_input_3`, `text`, **optional**): Choose a boolean value (e.g., true, false).
* **Decimal Input 3** (`decimal_input_3`, `number`, **optional**): Enter a decimal value.
