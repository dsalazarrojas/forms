<thinking>
The purpose of this form is to collect information for an Integrated Root Center Registration, likely for administrative or tracking purposes. This form seems to be designed to gather details about a program, its participants, and the center's operations. Before proceeding, ensure that you are not already registered and that this form is relevant to your situation.

To use this form effectively, follow these steps:

- Review the form's sections and fields to understand what information is required and what is optional.
- Fill out the form with accurate and complete information, especially for fields like program type, participant name, and contact details.
- Ensure that you select relevant options for fields like program type, participant type, and participant race/ethnicity.
- Double-check your answers before submitting the form to avoid errors.

Now, here is the help guide for the Integrated Root Center Registration form in Markdown format:

# Integrated Root Center Registration - Help Guide
## Purpose
The Integrated Root Center Registration form is used to collect information about an Integrated Root Center. This form is used for administrative and tracking purposes.

## How To Complete This Form
- Review the form's sections and fields before filling out the form.
- Fill out the form with accurate and complete information.
- Double-check your answers before submitting the form to avoid errors.

## Field-by-Field Explanation

* **Registration Form** (`registration_form_1`):
	+ A general text field for any additional information or comments.
* **Program Type** (`program_type`):
	+ Select the type of program you are registering (Program, Service, or Other).
* **Program Name** (`program_name`):
	+ Enter the name of the program you are registering.
* **Start Date** (`start_date`):
	+ Enter the start date of the program.
* **End Date** (`end_date`):
	+ Enter the end date of the program.
* **Contact Person** (`contact_person`):
	+ Enter the name of the person responsible for contact.
* **Contact Phone** (`contact_phone`):
	+ Enter the contact phone number of the person responsible for contact.
* **Contact Email** (`contact_email`):
	+ Enter the email address of the person responsible for contact.
* **Address** (`address`):
	+ Enter the address of the Integrated Root Center.
* **Notes** (`notes`):
	+ Enter any additional notes or comments.
* **Participant Count** (`participant_count`):
	+ Enter the number of participants in the program.
* **Program Description** (`program_description`):
	+ Enter a brief description of the program.
* **Enrollment Status** (`enrollment_status`):
	+ Select whether the program is Active or Inactive.
* **Active Status** (`active_status`):
	+ Select whether the program is active or not.
* **Registration Status** (`registration_status`):
	+ Select whether the program is registered or not.
* **Participant Type** (`participant_type`):
	+ Select the type of participant (Adult, Child, or Other).
* **Participant Name** (`participant_name`):
	+ Enter the name of the participant.
* **Participant Date of Birth** (`participant_dob`):
	+ Enter the date of birth of the participant.
* **Participant Age** (`participant_age`):
	+ Enter the age of the participant.
* **Participant Gender** (`participant_gender`):
	+ Select the gender of the participant.
* **Participant Race** (`participant_race`):
	+ Select the participant's race.
* **Participant Ethnicity** (`participant_ethnicity`):
	+ Select the participant's ethnicity.
* **Participant Contact Phone** (`participant_contact_phone`):
	+ Enter the contact phone number of the participant.
* **Participant Contact Email** (`participant_contact_email`):
	+ Enter the email address of the participant.
* **Submit** (`submit`):
	+ Confirm that you have filled out the form correctly before submitting.
