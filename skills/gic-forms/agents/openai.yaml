interface:
  display_name: "GIC Forms"
  short_description: "Manage hosted forms and response workflows"
  default_prompt: "Use $gic-forms to publish or manage a GIC form, generate an embed, install it into a site file, import it, or export responses."
