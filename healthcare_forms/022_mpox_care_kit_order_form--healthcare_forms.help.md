<thinking>

To create a user-friendly help guide for the "Mpox Care Kit Order Form", we need to understand the form's purpose and how users can complete it.

The form appears to be a comprehensive order form for healthcare-related supplies, including the details of the order, shipping, and payment. The form has multiple sections, including Order Details, Shipping Details, Payment Details, and Billing Details.

We'll break down the form into the following sections:

### Mpox Care Kit Order Form - Help Guide

#### Purpose
The Mpox Care Kit Order Form is a tool for suppliers to order healthcare-related supplies. It's essential to fill out this form accurately to ensure timely and correct delivery of the care kits.

#### How To Complete This Form

1.  **Fill out each field carefully**, ensuring that all required information is provided.
2.  **Review the entire form** before submitting to catch any errors or omissions.
3.  **Submit the form** to initiate the ordering process.

#### Field-by-Field Explanation

*   **Order Details**
    *   `order_details` (`1`, `text`, required: `false`): Enter a brief description of the order.
*   **Supplier Name**
    *   `supplier_name` (`2`, `text`, required: `false`): Enter the name of the supplier making the order.
*   **Supplier Email**
    *   `supplier_email` (`3`, `email`, required: `false`): Enter the email address of the supplier.
*   **Order Date**
    *   `order_date` (`4`, `date`, required: `false`): Enter the date the order was made.
*   **Order Time**
    *   `order_time` (`5`, `time`, required: `false`): Enter the time the order was made.
*   **Quantity Ordered**
    *   `quantity_ordered` (`6`, `number`, required: `false`): Enter the quantity of the care kit ordered.
*   **Order Status**
    *   `order_status` (`7`, `select_one`, required: `false`): Select the status of the order (Active or Inactive).
*   **Shipping Details**
    *   `shipping_details` (`8`, `text`, required: `false`): Enter any additional shipping instructions or details.
*   **Shipping Address**
    *   `shipping_address` (`9`, `text`, required: `false`): Enter the shipping address of the care kit.
*   **Shipping City**
    *   `shipping_city` (`10`, `text`, required: `false`): Enter the city for the shipping address.
*   **Shipping Province**
    *   `shipping_province` (`11`, `text`, required: `false`): Enter the province for the shipping address.
*   **Shipping Zip**
    *   `shipping_zip` (`12`, `text`, required: `false`): Enter the zip code for the shipping address.
*   **Shipping Country**
    *   `shipping_country` (`13`, `text`, required: `false`): Enter the country for the shipping address.
*   **Payment Details**
    *   `payment_details` (`14`, `text`, required: `false`): Enter any additional payment instructions or details.
*   **Invoice Number**
    *   `invoice_number` (`15`, `text`, required: `false`): Enter the invoice number for the order.
*   **Pox Care Kit Price**
    *   `pox_care_kit_price` (`16`, `number`, required: `false`): Enter the price of the care kit.
*   **Order Total**
    *   `order_total` (`17`, `number`, required: `false`): Enter the total cost of the order.
*   **Payment Method**
    *   `payment_method` (`18`, `select_one`, required: `false`): Select whether payment has been made (Yes or No).
*   **Payment Status**
    *   `payment_status` (`19`, `select_one`, required: `false`): Select the status of the payment (Active or Inactive).
*   **Notes**
    *   `notes` (`20`, `note`, required: `false`): Enter any additional notes or comments.
*   **Order Notes**
    *   `order_notes` (`21`, `note`, required: `false`): Enter any additional order-specific notes.
*   **Billing Details**
    *   `billing_details` (`22`, `text`, required: `false`): Enter any additional billing instructions or details.
*   **Billing First Name**
    *   `billing_first_name` (`23`, `text`, required: `false`): Enter the billing first name.
*   **Billing Last Name**
    *   `billing_last_name` (`24`, `text`, required: `false`): Enter the billing last name.
*   **Billing Address**
    *   `billing_address` (`25`, `text`, required: `false`): Enter the billing address.

#### Tips

*   Double-check the form for any errors or omissions before submitting.
*   Make sure to fill out all required fields accurately.
*   If you're unsure about any field, refer to the provided help guide or contact the supplier for guidance.

This help guide is designed to be user-friendly and easy to follow. If you have any further questions or need clarification on any of the fields, please don't hesitate to reach out to the supplier for assistance.
