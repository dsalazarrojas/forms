# Summer School Application Form - Help Guide
## Purpose

The Summer School Application Form is used to collect information from students who are applying for the summer school program. The form is designed to capture key details such as contact information, application status, payment method, and tuition fees information.

## How To Complete This Form

To complete the form, follow these steps:

1. Fill in the required fields marked with `*` (asterisk) symbol.
2. Double-check that you have filled all required fields before submitting the form.
3. Review each field for any additional instructions or specific requirements.

## Field-by-Field Explanation

* **First Name (`1`, `text`, required: `false`):**
	+ Enter your first name here. This will be used as a reference point for other fields and will appear on your application record.
* **Last Name (`2`, `text`, required: `false`):**
	+ Enter your last name here. This will be used as a reference point for other fields and will appear on your application record.
* **Email (`3`, `email`, required: `false`):**
	+ Enter your email address here. This will be used for communication and will appear on your application record.
* **Student ID (`4`, `number`, required: `false`):**
	+ Enter your student ID here. This is for internal reference and will appear on your application record.
* **Tuition Fees (`5`, `number`, required: `false`):**
	+ Enter the amount of the tuition fees here. This will be used to determine your eligibility for a certain amount of funding.
* **Select one (`6`, `select_one`, required: `true`):**
	+ Choose one option from the dropdown menu. This option will affect the processing of your application.
* **Select Multiple (`7`, `select_multiple`, required: `false`):**
	+ Choose multiple options from the dropdown menu. This option will allow you to select multiple values for your application.
* **Date (`8`, `date`, required: `false`):**
	+ Enter the date of your application here. This will be used to track the progress of your application.
* **Time (`9`, `time`, required: `false`):**
	+ Enter the time of your application here. This will be used for scheduling purposes.
* **Note (`10`, `note`, required: `false`):**
	+ Enter any additional notes about your application here. This will be used for internal reference.
* **Phone (`11`, `text`, required: `false`):**
	+ Enter your phone number here. This will be used for communication purposes.
* **Category (`12`, `select_one`, required: `true`):**
	+ Choose one option from the dropdown menu. This will affect the processing of your application.
* **School Name (`13`, `text`, required: `false`):**
	+ Enter the name of the school here. This will be used for internal reference.
* **Address (`14`, `text`, required: `false`):**
	+ Enter the address of the school here. This will be used for internal reference.
* **Tuition Fees via (`15`, `select_one`, required: `true`):**
	+ Choose one option from the dropdown menu. This will affect the processing of your application.
* **Amount (`16`, `number`, required: `false`):**
	+ Enter the amount of tuition fees here. This will be used to determine your eligibility for a certain amount of funding.
* **Accept Date (`17`, `date`, required: `false`):**
	+ Enter the date of acceptance here. This will be used to track the progress of your application.
* **Status (`18`, `select_multiple`, required: `false`):**
	+ Choose multiple options from the dropdown menu. This will affect the processing of your application.
* **Accept Date Status (`19`, `select_multiple`, required: `false`):**
	+ Choose multiple options from the dropdown menu. This will affect the processing of your application.
* **Application Type (`20`, `select_multiple`, required: `true`):**
	+ Choose multiple options from the dropdown menu. This will determine the type of application you are submitting.
* **Application Date (`21`, `date`, required: `false`):**
	+ Enter the date of your application here. This will be used to track the progress of your application.
* **Payment Method (`22`, `select_one`, required: `true`):**
	+ Choose one option from the dropdown menu. This will affect the processing of your application.
* **Application Status (`23`, `select_multiple`, required: `false`):**
	+ Choose multiple options from the dropdown menu. This will affect the processing of your application.
* **Tuition Fees Method (`24`, `select_multiple`, required: `true`):**
	+ Choose multiple options from the dropdown menu. This will determine the method of payment for your tuition fees.
* **School Application ID (`25`, `text`, required: `false`):**
	+ Enter any school application ID here. This will be used for internal reference.

## Tips

* Ensure that you have filled all required fields before submitting the form.
* Double-check your information for accuracy before submitting.
* If you have any questions or need assistance, contact our support team.
* This form is for legitimate school applications only.
