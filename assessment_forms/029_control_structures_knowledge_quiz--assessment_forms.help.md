<thinking>...</thinking>

# Control Structures Knowledge Quiz - Help Guide
## Purpose
This form is designed to assess your knowledge of control structures in programming, including sequences, selections, and iterations. It evaluates your understanding of concepts such as loops, conditional statements, and control flow.

## How To Complete This Form
To complete this form, follow these steps:

1. Enter your name and email address on the first page.
2. Select the correct answer for each question on the subsequent pages, based on your knowledge of control structures.
3. Answer questions in the sequence provided, which are divided into sections: Sequence and Selection, Iteration (Loops), Advanced Concepts, Code Reading, Best Practices, and Self-Assessment.

## Field-by-Field Explanation

* **Full Name** (text, required): Enter your name as it appears on your ID, passport, or other government-issued ID.
* **Email Address** (email, required): Enter a valid email address to receive the results of this assessment.
* **Institution or Organization** (text, optional): If you are representing an institution or organization, enter its name here.
* **Programming Experience Level** (select_one, required): Choose the level that best describes your experience in programming:
	+ Beginner
	+ Intermediate
	+ Advanced
	+ Expert
* **Sequence and Selection --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **What is a sequence control structure?** (select_one, required): Choose one of the following:
	+ Code executed in order from top to bottom
	+ A way to repeat code
	+ A decision-making structure
	+ A data storage method
* **Which statement is used for binary selection?** (select_one, required): Choose one of the following:
	+ if-else
	+ for loop
	+ while loop
	+ switch case
* **What does a switch statement do?** (select_one, required): Choose one of the following:
	+ Selects one case from multiple options
	+ Loops through an array
	+ Creates a new variable
	+ Exits a function
* **What is nested selection?** (select_one, required): Choose one of the following:
	+ An if statement inside another if statement
	+ Multiple loops combined
	+ A switch with cases
	+ Sequential execution
* **Iteration (Loops) --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **What is a loop in programming?** (select_one, required): Choose one of the following:
	+ A structure that repeats code
	+ A conditional statement
	+ A variable declaration
	+ A function call
* **Which loop checks condition before executing?** (select_one, required): Choose one of the following:
	+ while loop
	+ do-while loop
	+ for loop
	+ foreach loop
* **Which loop guarantees at least one execution?** (select_one, required): Choose one of the following:
	+ do-while loop
	+ while loop
	+ for loop
	+ nested loop
* **What is an infinite loop?** (select_one, required): Choose one of the following:
	+ A loop that never terminates
	+ A loop that runs once
	+ A loop with no body
	+ A loop with multiple conditions
* **What is the purpose of a for loop?** (select_one, required): Choose one of the following:
	+ To iterate a specific number of times
	+ To make decisions
	+ To store data
	+ To define functions
* **What does the break statement do?** (select_one, required): Choose one of the following:
	+ Exits a loop or switch
	+ Continues to next iteration
	+ Restarts the loop
	+ Pauses execution
* **What does the continue statement do?** (select_one, required): Choose one of the following:
	+ Skips to next iteration
	+ Exits the loop
	+ Restarts the program
	+ Stops all loops
* **Iteration (Loops) --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **What is a nested loop?** (select_one, required): Choose one of the following:
	+ A loop inside another loop
	+ Two loops side by side
	+ A loop with multiple conditions
	+ An infinite loop
* **What is loop unrolling?** (select_one, required): Choose one of the following:
	+ Optimization technique to reduce iterations
	+ A type of infinite loop
	+ Breaking out of nested loops
	+ Converting while to for loop
* **What is a sentinel value?** (select_one, required): Choose one of the following:
	+ A special value that terminates a loop
	+ The starting value of a loop
	+ The increment value
	+ A loop counter
* **What is recursion?** (select_one, required): Choose one of the following:
	+ A function calling itself
	+ A loop within a function
	+ Multiple functions calling each other
	+ An infinite loop
* **Code Reading --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **How many times does this loop execute - for i in range 5?** (select_one, required): Choose one of the following:
	+ 5 times
	+ 4 times
	+ 6 times
	+ Infinite
* **What is the output of a while loop with condition always true?** (select_one, required): Choose one of the following:
	+ Infinite loop
	+ Runs once
	+ Never runs
	+ Error
* **What happens after each iteration in a for loop?** (select_one, required): Choose one of the following:
	+ Update expression is executed
	+ Loop terminates
	+ Condition is ignored
	+ Program exits
* **Best Practices --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **What is the best practice for loop counters?** (select_one, required): Choose one of the following:
	+ Use meaningful variable names
	+ Always use i j k
	+ Use single letters
	+ No naming convention needed
* **How do you prevent off-by-one errors?** (select_one, required): Choose one of the following:
	+ Carefully check loop boundaries
	+ Always use less than
	+ Always use less than or equal
	+ Avoid loops entirely
* **Self-Assessment --** (note): This section is not directly answerable and only provides an introduction to the next sections.
* **How confident do you feel about control structures?** (select_one, required): Choose one of the following:
	+ Very Confident
	+ Somewhat Confident
	+ Neutral
	+ Not Very Confident
	+ Not Confident at All
* **Topics you need to review** (select_multiple, required): Choose all that apply:
	+ If-else statements
	+ Switch cases
	+ For loops
	+ While loops
	+ Nested structures
	+ Break and continue
	+ None - I feel confident
