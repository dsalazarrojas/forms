</thinking>

The primary goal of this form is to gather information about potential HPC (High-Performance Computing) cluster configurations to be set up within an organization. This will help the support team determine the most suitable options to offer support and services to the cluster users.
We are collecting information to understand the user's department and expected cluster size to better tailor support and services offered.

# HPC Cluster Setup Survey - Help Guide

## Purpose
This form is designed to collect information about your HPC cluster setup needs and expectations. It will help us understand your requirements and provide you with the best possible support and services.

## How To Complete This Form

1. Please select your user department:
   - Choose your user department from the list provided.
2. Enter expected cluster size:
   - Type the expected number of users that will be using the cluster.
3. Enter range of expected cluster size:
   - Provide a range (e.g., 10-50) if the expected cluster size is not fixed.
4. Enter cluster name:
   - Give your cluster a name that will be used for identification purposes.
5. Select cluster type:
   - Choose the type of cluster that suits your needs (General-Purpose, Research, Development, or Other).
6. Select operating system:
   - Choose the operating system that will be used on the cluster (CentOS, SUSE, or Other).
7. Enter storage capacity:
   - Type the total storage capacity required for the cluster.
8. Enter range of storage capacity:
   - Provide a range (e.g., 100-500) if the storage capacity is not fixed.
9. Select network connectivity:
   - Choose whether the cluster will be connected to the network (True or False).
10. Select support level:
    - Choose the level of support required (High, Medium, Low, or Other).
11. Select support team:
    - Choose the support team that will be responsible for the cluster (IT, Research, Engineering, or Other).
12. Select support contact:
    - Choose the primary contact method for support (Email, Phone, or Other).
13. Select support language:
    - Choose the language used for support (English, French, or Other).
14. Select support frequency:
    - Choose how often you expect support (Daily, Weekly, Monthly, or Other).
15. Enter support time:
    - Type the start and end times for support availability.
16. Enter range of support time:
    - Provide a range (e.g., 9:00 AM-5:00 PM) if support time is not fixed.
17. Select support level 2:
    - Choose the level of support required for support level 2 (High, Medium, Low, or Other).
18. Select support level 3:
    - Choose the level of support required for support level 3 (High, Medium, Low, or Other).
19. Select support level 4:
    - Choose the level of support required for support level 4 (High, Medium, Low, or Other).
20. Enter HPC cluster setup survey 2 (if needed):
    - Provide any additional comments about the cluster setup.
21. Select cluster environment:
    - Choose the environment where the cluster will be set up (On-premises, Public cloud, or Other).
22. Enter range of cluster environment:
    - Provide a range (e.g., on-premises only, cloud only) if the cluster environment is not fixed.
23. Enter cluster setup deadline:
    - Type the date by which the cluster needs to be set up.
24. Enter range of cluster setup deadline:
    - Provide a range (e.g., 2 weeks, 1 month) if the cluster setup deadline is not fixed.

## Field-by-Field Explanation

* **User department** (`user_department`, `select_one`, required/optional): Choose your user department from the provided list to help us understand your department's specific needs.
* **Expected cluster size** (`expected_cluster_size`, `number`, required/optional): Type the expected number of users that will be using the cluster.
* **Range of expected cluster size** (`expected_cluster_size_range`, `text`, required/optional): Provide a range (e.g., 10-50) if the expected cluster size is not fixed.
* **Cluster name** (`cluster_name`, `text`, required/optional): Give your cluster a name that will be used for identification purposes.
* **Cluster type** (`cluster_type`, `select_one`, required/optional): Choose the type of cluster that suits your needs (General-Purpose, Research, Development, or Other).
* **Operating System** (`operating_system`, `select_one`, required/optional): Choose the operating system that will be used on the cluster (CentOS, SUSE, or Other).
* **Storage capacity** (`storage_capacity`, `number`, required/optional): Type the total storage capacity required for the cluster.
* **Range of storage capacity** (`storage_capacity_range`, `text`, required/optional): Provide a range (e.g., 100-500) if the storage capacity is not fixed.
* **Network connectivity** (`network_connectivity`, `select_one`, required/optional): Choose whether the cluster will be connected to the network (True or False).
* **Support level** (`support_level`, `select_one`, required/optional): Choose the level of support required (High, Medium, Low, or Other).
* **Support team** (`support_team`, `select_one`, required/optional): Choose the support team that will be responsible for the cluster (IT, Research, Engineering, or Other).
* **Support contact** (`support_contact`, `select_one`, required/optional): Choose the primary contact method for support (Email, Phone, or Other).
* **Support language** (`support_language`, `select_one`, required/optional): Choose the language used for support (English, French, or Other).
* **Support frequency** (`support_frequency`, `select_one`, required/optional): Choose how often you expect support (Daily, Weekly, Monthly, or Other).
* **Support time** (`support_time`, `time`, required/optional): Type the start and end times for support availability.
* **Range of support time** (`support_time_range`, `text`, required/optional): Provide a range (e.g., 9:00 AM-5:00 PM) if support time is not fixed.
* **Support level 2** (`support_level_2`, `select_one`, required/optional): Choose the level of support required for support level 2 (High, Medium, Low, or Other).
* **Support level 3** (`support_level_3`, `select_one`, required/optional): Choose the level of support required for support level 3 (High, Medium, Low, or Other).
* **Support level 4** (`support_level_4`, `select_one`, required/optional): Choose the level of support required for support level 4 (High, Medium, Low, or Other).
* **HPC cluster setup survey 2** (`hpc_cluster_setup_survey_2`, `text`, required/optional): Provide any additional comments about the cluster setup.
* **Cluster environment** (`cluster_environment`, `select_one`, required/optional): Choose the environment where the cluster will be set up (On-premises, Public cloud, or Other).
* **Range of cluster environment** (`cluster_environment_range`, `text`, required/optional): Provide a range (e.g., on-premises only, cloud only) if the cluster environment is not fixed.
* **Cluster setup deadline** (`cluster_setup_deadline`, `date`, required/optional): Type the date by which the cluster needs to be set up.
* **Range of cluster setup deadline** (`cluster_setup_deadline_range`, `text`, required/optional): Provide a range (e.g., 2 weeks, 1 month) if the cluster setup deadline is not fixed.

## Tips

* Please provide accurate information to help us better understand your needs.
* If you are unsure about any field, please ask for clarification.
* If you need to provide more information or clarification, please use the "HPC cluster setup survey 2" field.
