# Cyber Security Quiz For Employees - Help Guide
## Purpose
This form is a quiz to assess your cyber security practices as an employee. Please answer all the questions honestly to get a clear picture of your current level of cyber security awareness and knowledge.

## How To Complete This Form
1. Answer all the questions in each page of the quiz.
2. For each question, select one of the options provided.
3. If you're unsure or don't know the answer, please mark it as "False" or "Never" as appropriate.
4. Submit the form to complete the quiz.

## Field-by-Field Explanation

* **Employee Name** (`employee_name`, text, optional): Please enter your name as it appears in our company's records.
* **Role** (`role`, select_one, optional): Select one of the following roles:
	+ IT
	+ Management
	+ Support
* **Have you completed the security quiz in the last 6 months?** (`quiz_completed`, select_one, optional):
	+ 'True'
	+ 'False'
* **Password Manager** (`password_manager`, select_one, optional):
	+ 'True'
	+ 'False'
* **Password Manager Tool** (`password_manager_tool`, select_multiple, optional):
	+ LastPass
	+ 1Password
	+ KeePass
	+ Dashlane
* **Do you use Last 2FA?** (`last_2fa`, select_one, optional):
	+ 'True'
	+ 'False'
* **Do you rotate your passwords?** (`password_rotation`, select_one, optional):
	+ 'True'
	+ 'False'
* **How often do you rotate your passwords?** (`password_rotation_frequency`, select_one, optional):
	+ Daily
	+ Weekly
	+ Monthly
	+ Never
* **What is the average length of your password?** (`password_length`, number, optional):
	+ (please enter a number)
* **Have you received phishing training?** (`phishing_training`, select_one, optional):
	+ 'True'
	+ 'False'
* **Have you ever fallen victim to phishing attacks?** (`phishing_attacks`, select_one, optional):
	+ 'True'
	+ 'False'
* **Have you received security training?** (`security_training`, select_one, optional):
	+ 'True'
	+ 'False'
* **Have you attended a security conference?** (`security_conference`, select_one, optional):
	+ 'True'
	+ 'False'
* **Do you have a security buddy?** (`security_buddy`, select_one, optional):
	+ 'True'
	+ 'False'
* **Security Buddy Name** (`security_buddy_name`, text, optional): (only if you have a security buddy)
* **When was last your password updated?** (`last_password_update`, date, optional):
	+ (please enter a date in the format MM/DD/YYYY)
* **How strong is your password?** (`password_strength`, number, optional):
	+ (please enter a number between 1-10)
* **How often is your password expire?** (`password_expiration`, select_one, optional):
	+ 'True'
	+ 'False'
* **How often is your password expire?** (`password_expiration_frequency`, select_one, optional):
	+ Daily
	+ Weekly
	+ Monthly
	+ Never
* **When was your password manager last updated?** (`password_manager_last_updated`, date, optional):
	+ (please enter a date in the format MM/DD/YYYY)
* **Last Password Changed?** (`password_last_change`, select_one, optional):
	+ 'True'
	+ 'False'
* **Do you follow the security policy of our company?** (`security_policy`, select_one, optional):
	+ 'True'
	+ 'False'
* **Password Manager Password** (`password_manager_password`, select_multiple, optional):
	+ LastPass
	+ 1Password
	+ KeePass
	+ Dashlane
* **Two Factor Authentication?** (`two_factor_authentication`, select_one, optional):
	+ 'True'
	+ 'False'
* **How many security questions do you have?** (`security_questions`, select_one, optional):
	+ 1
	+ 2
	+ 3
	+ More than 3

## Tips
- Please answer all questions honestly.
- If you're unsure or don't know the answer, please mark it as "False" or "Never" as appropriate.
- Submit the form to complete the quiz.
