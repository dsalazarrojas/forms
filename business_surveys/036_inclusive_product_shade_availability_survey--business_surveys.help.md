# Inclusive Product Shade Availability Survey - Help Guide
## Purpose
This form is designed to help you gather information about the availability of different shades of a product, including whether they are available, their match with other attributes, and so on.

## How To Complete This Form
To complete this form, please follow these steps:

1. Review each question carefully and answer all the questions.
2. Select the relevant options for each question based on the availability of the product shades.
3. Make sure to answer all questions, even if you're not sure about some of them.

## Field-by-Field Explanation
* **Introduction** (`introduction`, `note`, optional): This is a note section where you can provide any additional information or context about the product and its shades.
* **Product Category** (`product_category`, `select_one`, optional): Select `Yes` if the product category is available, or `No` if it is not.
* **Color Family** (`color_family`, `select_multiple`, optional): Select the color family that matches your product, if any, by checking the corresponding boxes.
* **Color Name** (`color_name`, `text`, optional): Enter the color name that matches your product, if any.
* **Shade** (`shade`, `text`, optional): Enter the shade name that matches your product, if any.
* **Finish** (`finish`, `select_one`, optional): Select `Yes` if the finish is available, or `No` if it is not.
* **Color** (`color`, `text`, optional): Enter the color name that matches your product, if any.
* **Size** (`size`, `text`, optional): Enter the size that matches your product, if any.
* **Packaging** (`packaging`, `text`, optional): Enter the packaging that matches your product, if any.
* **Availability** (`availability`, `select_multiple`, optional): Select the availability options that match your product, such as `Yes` for available, `No` for not available, and so on.
* **Shade Availability** (`shade_availability`, `select_one`, optional): Select `Yes` if the shade is available, or `No` if it is not.
* **Color Match** (`color_match`, `select_multiple`, optional): Select the color match options that match your product, such as `Yes` for matches and `No` for does not match.
* **Color Name Match** (`color_name_match`, `text`, optional): Enter the color name match that matches your product, if any.
* **Color Shade Availability** (`color_shade_availability`, `select_one`, optional): Select `Yes` if the color shade is available, or `No` if it is not.
* **Color Shade Match** (`color_shade_match`, `select_multiple`, optional): Select the color shade match options that match your product, if any.
* **Color Family Match** (`color_family_match`, `select_multiple`, optional): Select the color family match options that match your product, if any.
* **Color Finish Match** (`color_finish_match`, `select_multiple`, optional): Select the color finish match options that match your product, if any.
* **Shade Match** (`shade_match`, `select_multiple`, optional): Select the shade match options that match your product, if any.
* **Finish Match** (`finish_match`, `select_multiple`, optional): Select the finish match options that match your product, if any.
* **Color Match Shade** (`color_match_shade`, `select_multiple`, optional): Select the color match shade options that match your product, if any.
* **Size Match** (`size_match`, `select_multiple`, optional): Select the size match options that match your product, if any.
* **Packaging Match** (`packaging_match`, `select_multiple`, optional): Select the packaging match options that match your product, if any.
* **Availability Match** (`availability_match`, `select_multiple`, optional): Select the availability match options that match your product, if any.
* **Availability Shade** (`availability_shade`, `select_multiple`, optional): Select the availability shade options that match your product, if any.
* **Availability Color** (`availability_color`, `select_multiple`, optional): Select the availability color options that match your product, if any.

Note: Please make sure to review all fields and answer all questions carefully, as some questions may require more specific information than others.
