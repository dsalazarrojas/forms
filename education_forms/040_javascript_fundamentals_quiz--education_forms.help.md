# JavaScript Fundamentals Quiz - Help Guide
## Purpose
The JavaScript Fundamentals Quiz is a 25-question multiple-choice quiz designed to test your knowledge and understanding of basic programming concepts in JavaScript. The quiz covers topics such as variables, operators, and control structures, and is intended to help you improve your programming skills.

## How To Complete This Form
1. Read each question carefully and understand what is being asked.
2. For questions that require code writing, type the code into the text field provided.
3. For multiple-choice questions, select the correct answer from the options provided.
4. Make sure to answer each question carefully, as some questions may have multiple correct answers.

## Field-by-Field Explanation

### First Question
* **First Question** (`user_input_1`, `text`, required/optional): No explanation provided, as it's not a multiple-choice question.

### What is the correct syntax to create an array in JavaScript?
* **What is the correct syntax to create an array in JavaScript?** (`user_input_2`, `text`, required/optional): This question asks for the correct syntax to create an array in JavaScript. You can answer this question by writing the correct JavaScript code to create an array.

### What is the output of the code 2 + 2?
* **What is the output of the code 2 + 2?** (`user_input_3`, `text`, required/optional): This question asks for the output of the code `2 + 2`. Type the correct output into the text field.

### What is the output of the code 5 + 1?
* **What is the output of the code 5 + 1?** (`user_input_4`, `text`, required/optional): This question asks for the output of the code `5 + 1`. Type the correct output into the text field.

### What is the correct order of operations in the code 1 + 2 * 3?
* **What is the correct order of operations in the code 1 + 2 * 3?** (`user_input_5`, `text`, required/optional): This question asks for the correct order of operations in the code `1 + 2 * 3`. Type the correct order of operations into the text field.

### Select the correct variable in the code var x = 5; if x  10 { console.log('x is 5'); }
* **Select the correct variable in the code var x = 5; if x  10 { console.log('x is 5'); }** (`user_input_6`, `select_one`, required/optional): This question asks you to select the correct variable `x` from the options provided.

### Select all correct answers
* **Select all correct answers** (`user_input_7`, `select_multiple`, required/optional): This question asks you to select all correct answers.

### What is the correct output of the code var i = 2; var j = 2;
* **What is the correct output of the code var i = 2; var j = 2;** (`user_input_8`, `text`, required/optional): This question asks for the output of the code `var i = 2; var j = 2;`. Type the correct output into the text field.

### What is the correct output of the code var a = 5; var b = 10;
* **What is the correct output of the code var a = 5; var b = 10;** (`user_input_9`, `text`, required/optional): This question asks for the output of the code `var a = 5; var b = 10;`. Type the correct output into the text field.

### What is the output of the code 5 + 5?
* **What is the output of the code 5 + 5?** (`user_input_10`, `text`, required/optional): This question asks for the output of the code `5 + 5`. Type the correct output into the text field.

### What is the correct output of the code a = b + 5?
* **What is the correct output of the code a = b + 5?** (`user_input_11`, `text`, required/optional): This question asks for the output of the code `a = b + 5;`. Type the correct output into the text field.

### What is the correct output of the code a = 5 + 5?
* **What is the correct output of the code a = 5 + 5?** (`user_input_12`, `text`, required/optional): This question asks for the output of the code `a = 5 + 5;`. Type the correct output into the text field.

### What is the correct output of the code b = 5 + 3?
* **What is the correct output of the code b = 5 + 3?** (`user_input_13`, `text`, required/optional): This question asks for the output of the code `b = 5 + 3;`. Type the correct output into the text field.

### What is the correct output of the code a = b + 4?
* **What is the correct output of the code a = b + 4?** (`user_input_14`, `text`, required/optional): This question asks for the output of the code `a = b + 4;`. Type the correct output into the text field.

### What is the correct output of the code a = 5 * 5?
* **What is the correct output of the code a = 5 * 5?** (`user_input_15`, `text`, required/optional): This question asks for the output of the code `a = 5 * 5;`. Type the correct output into the text field.

### What is the correct output of the code b = 5 + 4?
* **What is the correct output of the code b = 5 + 4?** (`user_input_16`, `text`, required/optional): This question asks for the output of the code `b = 5 + 4;`. Type the correct output into the text field.

### What is the correct output of the code a = (5 + 5)?
* **What is the correct output of the code a = (5 + 5)?** (`user_input_17`, `text`, required/optional): This question asks for the output of the code `a = (5 + 5);`. Type the correct output into the text field.

### What is the correct output of the code b = 3 + 5?
* **What is the correct output of the code b = 3 + 5?** (`user_input_18`, `text`, required/optional): This question asks for the output of the code `b = 3 + 5;`. Type the correct output into the text field.

### What is the correct output of the code a = 5 * (5 + 5)?
* **What is the correct output of the code a = 5 * (5 + 5)?** (`user_input_19`, `text`, required/optional): This question asks for the output of the code `a = 5 * (5 + 5);`. Type the correct output into the text field.

### What is the correct output of the code b = 5 % 4?
* **What is the correct output of the code b = 5 % 4?** (`user_input_20`, `text`, required/optional): This question asks for the output of the code `b = 5 % 4;`. Type the correct output into the text field.

### What is the correct output of the code 5 + 3?
* **What is the correct output of the code 5 + 3?** (`user_input_21`, `text`, required/optional): This question asks for the output of the code `5 + 3;`. Type the correct output into the text field.

### What is the correct output of the code a = b + 4?
* **What is the correct output of the code a = b + 4?** (`user_input_22`, `text`, required/optional): This question asks for the output of the code `a = b + 4;`. Type the correct output into the text field.

### What is the correct output of the code b = a + 4?
* **What is the correct output of the code b = a + 4?** (`user_input_23`, `text`, required/optional): This question asks for the output of the code `b = a + 4;`. Type the correct output into the text field.

### What is the correct output of the code a = 5 + b?
* **What is the correct output of the code a = 5 + b?** (`user_input_24`, `text`, required/optional): This question asks for the output of the code `a = 5 + b;`. Type the correct output into the text field.

### What is the correct output of the code b = 5 + a?
* **What is the correct output of the code b = 5 + a?** (`user_input_25`, `text`, required/optional): This question asks for the output of the code `b = 5 + a;`. Type the correct output into the text field.
