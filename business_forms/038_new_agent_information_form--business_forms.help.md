<thinking>
This form is designed to collect information from new agents, which can include job seekers, agents, or other individuals representing a business or organization. The purpose of this form is to gather essential details about the agent, such as their contact information, work history, and qualifications. This form is likely used for internal purposes, such as creating a database or managing relationships with external partners.

Before proceeding, please ensure that you are filling out this form for a new agent and not for an existing one. If you are filling out this form for an existing agent, please use the corresponding "agent update form" instead.

Also, note that the form fields are mostly self-explanatory, but if you are unsure about any field, please refer to this guide for further clarification.

</thinking>

# new_agent_information_form - Help Guide
## Purpose

This form is designed to collect information from new agents, including job seekers, agents, or other individuals representing a business or organization.

## How To Complete This Form

1. Fill out the form completely and accurately, providing all required information.
2. Ensure that you are filling out this form for a new agent and not for an existing one.
3. Review each field carefully and enter the required information.

## Field-by-Field Explanation

- **First Name** (`first_name`, `text`, required): Enter the first name of the new agent.
  - This field is required to collect the first name of the agent.
  - Please enter the first name as it appears on the agent's identification documents.

- **Last Name** (`last_name`, `text`, required): Enter the last name of the new agent.
  - This field is required to collect the last name of the agent.
  - Please enter the last name as it appears on the agent's identification documents.

- **Job Title** (`job_title`, `text`, required): Enter the job title or position held by the new agent.
  - This field is required to collect the job title of the agent.
  - Please enter the job title as it appears on the agent's identification documents.

- **Other Job Title** (`job_title_other`, `text`, optional): Enter any other job title or position held by the new agent.
  - This field is optional and only required if the agent has multiple job titles.

- **Date of Birth** (`date_of_birth`, `date`, optional): Enter the date of birth of the new agent (in MM/DD/YYYY format).
  - This field is optional and only required if it's necessary for the agent's identification or other purposes.

- **Work History** (`work_history`, `text`, optional): Enter a brief description of the new agent's work history.
  - This field is optional and only required if it's necessary for the agent's background information.

- **Background** (`background`, `text`, optional): Enter a brief description of the new agent's background or experience.
  - This field is optional and only required if it's necessary for the agent's background information.

- **Interests** (`interests`, `text`, optional): Enter a brief description of the new agent's interests or hobbies.
  - This field is optional and only required if it's necessary for the agent's personal information.

- **Experience** (`experience`, `text`, optional): Enter a brief description of the new agent's work experience.
  - This field is optional and only required if it's necessary for the agent's work history.

- **Education** (`education`, `text`, optional): Enter a brief description of the new agent's education background.
  - This field is optional and only required if it's necessary for the agent's background information.

- **Email** (`email`, `email`, required): Enter the new agent's email address (in the format of `username@example.com`).
  - This field is required to collect the new agent's contact email.

- **Phone** (`phone`, `text`, required): Enter the new agent's phone number.
  - This field is required to collect the new agent's contact phone number.

- **Work Phone** (`work_phone`, `text`, required): Enter the new agent's work phone number.
  - This field is required to collect the new agent's work contact number.

- **Personal Phone** (`personal_phone`, `text`, required): Enter the new agent's personal phone number.
  - This field is required to collect the new agent's personal contact number.

- **Work Email** (`work_email`, `email`, required): Enter the new agent's work email address (in the format of `username@example.com`).
  - This field is required to collect the new agent's work email.

- **Personal Email** (`personal_email`, `email`, optional): Enter the new agent's personal email address (in the format of `username@example.com`).
  - This field is optional and only required if it's necessary for the agent's personal contact information.

- **Address** (`address`, `text`, required): Enter the new agent's home address.
  - This field is required to collect the new agent's contact address.

- **Country** (`country`, `select_one`, optional): Select the country of the new agent's origin (if necessary).
  - This field is optional and only required if it's necessary for the agent's background information.

- **State** (`state`, `text`, optional): Enter the new agent's state of origin (if necessary).
  - This field is optional and only required if it's necessary for the agent's background information.

- **City** (`city`, `text`, required): Enter the new agent's city of origin.
  - This field is required to collect the new agent's city information.

- **Postal Code** (`postal_code`, `text`, optional): Enter the new agent's postal code or zip code (if necessary).
  - This field is optional and only required if it's necessary for the address information.

- **Work Start Date** (`work_start_date`, `date`, optional): Enter the new agent's start date of work (if necessary).
  - This field is optional and only required if it's necessary for the agent's work history.

- **Work End Date** (`work_end_date`, `date`, optional): Enter the new agent's end date of work (if necessary).
  - This field is optional and only required if it's necessary for the agent's work history.

- **Job Category** (`job_category`, `select_multiple`, optional): Select the job category of the new agent (if necessary).
  - This field is optional and only required if it's necessary for the agent's background information.

- **Job Type** (`job_type`, `select_multiple`, optional): Select the job type of the new agent (if necessary).
  - This field is optional and only required if it's necessary for the agent's background information.

## Tips

- Please fill out the form completely and accurately.
- Ensure that you are filling out this form for a new agent and not for an existing one.
- If you are unsure about any field, refer to this guide for further clarification.
