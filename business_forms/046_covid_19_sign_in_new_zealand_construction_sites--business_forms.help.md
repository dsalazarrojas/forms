<thinking>

To create an effective help guide for the Covid-19 Sign In New Zealand Construction Sites form, we need to first understand the purpose of the form and its requirements. The form is likely designed to record site entry and exit information for employees at construction sites in New Zealand, helping to maintain accurate and up-to-date records during the pandemic.

Upon examining the form's fields, we notice that most fields are related to recording site entry and exit information, including employee details (name, ID), entry and exit times, site details (ID, address, location), and postal codes. This suggests that the form is intended for site managers or administrators to collect data on employees entering and leaving construction sites.

We should also check if the form has any specific requirements or restrictions. Upon reviewing the form schema, it appears that most fields are optional, which may indicate that the form is primarily for data collection rather than for strict compliance or regulation purposes.

With this understanding, we can proceed to create the help guide.

# Covid 19 Sign In New Zealand Construction Sites - Help Guide

## Purpose

The purpose of this form is to record accurate information about employee entry and exit from construction sites in New Zealand, particularly for the duration of the Covid-19 pandemic. This includes capturing individual employee information, entry and exit times, site details, and other relevant site-specific data.

## How To Complete This Form

To use this form, please follow these steps:

1. Select the correct option from the site-in or site-out dropdowns (if available).
2. Input the employee's name and ID, if required for your organization.
3. Enter the time of site entry and exit using the format HH:MM for time fields.
4. Enter the site's details, including site ID, address, location, and postcode.
5. Optionally, input the employee's postcode for contact tracing purposes.
6. Review and submit the form for accurate and complete information.

## Field-by-Field Explanation

* **Site In** (`1`, `text`, required: `false`): This field is for selecting or entering the site where you are reporting your entry.
* **Site Out** (`2`, `text`, required: `false`): This field is for selecting or entering the site where you are reporting your exit.
* **Employee In Time** (`3`, `number`, required: `false`): Enter the time you entered the site in 24-hour format (HH:MM).
* **Employee Out Time** (`4`, `number`, required: `false`): Enter the time you exited the site in 24-hour format (HH:MM).
* **Employee In Name** (`5`, `text`, required: `false`): Input the name of the employee entering or leaving the site.
* **Employee Out Name** (`6`, `text`, required: `false`): Input the name of the employee entering or leaving the site (note that this may be redundant with Employee In Name, as it's likely the same person).
* **Site ID** (`7`, `number`, required: `false`): Enter the unique identifier for the site where you are entering or exiting.
* **Employee ID** (`8`, `number`, required: `false`): Enter the ID of the employee entering or leaving the site (if required by your organization).
* **Date** (`9`, `date`, required: `false`): Enter the date in YYYY-MM-DD format of site entry or exit.
* **Time** (`10`, `time`, required: `false`): Enter the time of site entry or exit in 24-hour format (HH:MM).
* **Site Location** (`11`, `text`, required: `false`): Enter the location or name of the construction site.
* **Site Address** (`12`, `text`, required: `false`): Enter the address of the construction site.
* **Site Postcode** (`13`, `text`, required: `false`): Enter the postcode of the construction site.
* **Employee Postcode** (`14`, `text`, required: `false`): Enter the postcode of the employee for contact tracing purposes.
