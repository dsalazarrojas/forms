<thinking>Before generating the help guide, I will review the provided YAML fields to ensure I understand the form's purpose and requirements.</thinking>

# Lease Renewal Checklist Form - Help Guide

## Purpose
This Lease Renewal Checklist Form is used to collect information for lease renewal purposes. It's essential to fill out this form accurately to ensure a smooth lease renewal process.

## How To Complete This Form

1.  Review the form carefully before completing it.
2.  Fill out the form with the required information.
3.  Ensure you provide accurate and complete answers to each field.

## Field-by-Field Explanation

*   **Lease Term Start Date** (`lease_term_start_date`, date, optional): Enter the start date of the lease term.
*   **Lease Term End Date** (`lease_term_end_date`, date, optional): Enter the end date of the lease term.
*   **Rent Amount** (`rent_amount`, number, optional): Enter the current rent amount.
*   **Lease Term Length** (`lease_term_length`, number, optional): Enter the length of the lease term.
*   **Tenant Name** (`tenant_name`, text, optional): Enter the tenant's name.
*   **Move In Date** (`move_in_date`, date, optional): Enter the date the tenant moved in.
*   **Move Out Date** (`move_out_date`, date, optional): Enter the date the tenant plans to move out.
*   **Lease Renewal Date** (`lease_renewal_date`, date, optional): Enter the date of the lease renewal.
*   **Lease Renewal Status** (`lease_renewal_status`, select_one, optional): Select whether the lease is renewing or not.
*   **Lease Status** (`lease_status`, select_multiple, optional): Select the current lease status (Active, Inactive, or Expired).
*   **Tenant Status** (`tenant_status`, select_multiple, optional): Select the current tenant status (Active, Inactive, or Expired).
*   **Notice Period** (`notice_period`, select_one, optional): Select whether a notice period is required or not.
*   **Rent Increase** (`rent_increase`, select_multiple, optional): Select whether there is a rent increase or not.
*   **Rent Type** (`rent_type`, select_one, optional): Select the type of rent (Rent or Lease).
*   **Lease Document** (`lease_document`, select_multiple, optional): Select the status of the lease document (Signed or Unsigned).
*   **Rent Increase Percentage** (`rent_increase_percentage`, number, optional): Enter the rent increase percentage.
*   **Tenant Occupation** (`tenant_occupation`, text, optional): Enter the tenant's occupation.
*   **Tenant Income** (`tenant_income`, number, optional): Enter the tenant's income.
*   **Lease Document Status** (`lease_document_status`, select_one, optional): Select the status of the lease document (Signed or Unsigned).
*   **Tenant Document Status** (`tenant_document_status`, select_one, optional): Select the status of the tenant document (Signed or Unsigned).
*   **Notice to Move Out Date** (`notice_to_move_out_date`, date, optional): Enter the date of the notice to move out.
*   **Lease Renewal Date 2** (`lease_renewal_date_2`, date, optional): Enter the date of the lease renewal 2.
*   **Tenant Document** (`tenant_document`, select_multiple, optional): Select the status of the tenant document (Signed or Unsigned).
*   **Notice Period 2** (`notice_period_2`, select_one, optional): Select whether a notice period is required or not.
