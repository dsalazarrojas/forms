<thinking>
To create a user-facing help guide for the "leak_repair_service_agreement" form, we need to ensure that the form's purpose, completion instructions, and field explanations are clear and concise. 

The form appears to be designed for reporting or requesting maintenance-related activities, likely for a leak repair service. We'll need to confirm that the form is not being used for other purposes and that the required fields are accurate.

The form has 14 fields, but some of them are optional. We'll need to focus on explaining each field's purpose and any specific instructions or limitations.

Assuming the form is used for leak repair service requests, we'll use that as our base understanding.

</thinking>

# leak_repair_service_agreement - Help Guide
## Purpose
This form is used to request a leak repair service. It guides the user in providing necessary information to schedule and complete the repair.

## How To Complete This Form
To complete this form, please follow these steps:
1. Fill out each field with the required information.
2. Ensure all required fields are filled out before submitting the form.
3. Review your submission for accuracy before sending it in.

## Field-by-Field Explanation

* **service_agreement** (`service_agreement`): Enter a brief description of the leak repair request.
* **leak_repair_details** (`leak_repair_details`): Provide more details about the leak, if necessary.
* **service_request** (`service_request`): Select the type of service requested:
	+ Option 1: Emergency leak repair
	+ Option 2: Scheduled leak repair
* **schedule_visit** (`schedule_visit`): Choose a date for the leak repair visit. This field is for scheduling the visit, but it's not required.
* **leak_location** (`leak_location`): Enter the location of the leak.
* **leak_type** (`leak_type`): Select the type of leak:
	+ Option 1: Water leak
	+ Option 2: Gas leak
	+ Option 3: Other
* **leak_description** (`leak_description`): Provide a detailed description of the leak.
* **schedule_date** (`schedule_date`): Choose a date for the leak repair visit. This field is for scheduling the visit, but it's not required.
* **schedule_time** (`schedule_time`): Choose a time for the leak repair visit. This field is for scheduling the visit, but it's not required.
* **contractor_name** (`contractor_name`): Enter the name of the contractor handling the repair.
* **contractor_phone** (`contractor_phone`): Enter the phone number of the contractor.
* **contractor_email** (`contractor_email`): Enter the email of the contractor.
* **leak_cost** (`leak_cost`): Enter the estimated cost of the repair.
* **payment_method** (`payment_method`): Select a payment method (this field might be used for payment instructions, but it's currently empty, so it's unclear its purpose).

Please note that the payment method field is currently empty and its purpose is unknown. If this field is intended for a specific payment method, please update its description accordingly.
