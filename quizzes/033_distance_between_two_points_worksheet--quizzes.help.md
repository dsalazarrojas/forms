This form is a simple worksheet for calculating the distance between two points in a two-dimensional space. It allows users to input the coordinates of two points, calculate the distance between them, and view the result. The form is designed to be straightforward and easy to use, with a minimum number of fields that correspond to the basic mathematical calculation.

## Distance Between Two Points Worksheet - Help Guide

## Purpose
The Distance Between Two Points Worksheet is a simple tool for calculating the distance between two points in a two-dimensional space. It's a basic mathematical concept that can be useful in various fields such as geometry, physics, and engineering.

## How To Complete This Form
To complete this form, follow these steps:

1. Enter the coordinates of the first point in the `x1` and `y1` fields.
2. Enter the coordinates of the second point in the `x2` and `y2` fields.
3. Calculate the distance between the two points by clicking on the "Submit" button, which will auto-generate the result in the `distance` field.

## Field-by-Field Explanation

* **distance between two points** (page 1):
	+ This is the title of the form that you are currently viewing.
* **x1** (1, number, required: false):
	+ Enter the x-coordinate of the first point.
* **y1** (2, text, required: false):
	+ Enter the y-coordinate of the first point.
* **x2** (3, text, required: false):
	+ Enter the x-coordinate of the second point.
* **y2** (4, text, required: false):
	+ Enter the y-coordinate of the second point.
* **x2-x1** (6, number, required: false):
	+ This field will auto-generate the difference between the x-coordinates of the two points.
* **y2-y1** (7, text, required: false):
	+ This field will auto-generate the difference between the y-coordinates of the two points.
* **distance** (8, number, required: false):
	+ This field will auto-generate the result of the distance calculation between the two points.

## Tips
- Make sure to enter the coordinates in the correct format (e.g. x1 should be a number, y1 should be a number, x2 should be a text representing an expression like "x1 + 1").
- The form will only auto-generate the distance result when both x1 and y1 coordinates are filled. If either x1 or y1 are empty, the form will display an error message. 

Please note that the provided YAML does not specify the "Submit" button, but it is implied as part of the "How To Complete This Form" section. 

This form is a basic calculator, so no "options" are available for any field. 

This guide will be helpful for users who want to use this form for simple distance calculations.
